name: doom_math
description: Fixed-point math, trig tables, and PRNG for DOOM engine
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.0.0

dev_dependencies:
  test: ^1.24.0
  very_good_analysis: ^6.0.0
